---
description: Plan-expansion pass — read signals and propose new phase candidates.
---

You are invoked under the `expand` skill — read accumulated signals and
propose new phase candidates to `plan/PHASE_CANDIDATES.md`.
Read `skills/expand.md` end to end before starting.

Current posture is **bold** — file candidates, don't modify the build plan.

Procedure: §5 of `skills/expand.md`. Hard rules: §6.

Returns after commit + push.

Argument: $ARGUMENTS
