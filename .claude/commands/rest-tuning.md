---
description: Rest encounter (The Night Watch) balance loop — seeded posture × policy probes over the pure engine to analyse the posture gradient, warmth economy, cleanse gating, and dawn-tier rates against the meagre-but-never-lethal doctrine; numeric changes + report via PR.
---

You are invoked under the `rest-tuning` skill — run one tuning tick
end-to-end. Read `skills/rest-tuning.md` in full before doing anything
else; it describes the surface read, the e2e + policy-probe evidence
matrix, the locked contracts (never lethal, two-way at claim only), the
numeric-only guardrails (dream prose is narrative surface — untouchable),
and the single-PR delivery.

Single-PR delivery: findings report, propose-only suggestions, and any
applied numeric/content-level changes all ride one new branch + PR opened
for review. Never approve, auto-merge, release, or push to `main`.

`--focus` narrows which axis the skill prioritises; without it, the skill
runs a full sweep of the targets table.

When invoked under `/loop`, the user is not present. Return cleanly after
the tick completes.

Argument: $ARGUMENTS
