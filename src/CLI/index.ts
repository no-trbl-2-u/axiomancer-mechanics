/**
 * CLI Module
 * 
 * This module contains command-line interface scripts for testing
 * and simulating various game mechanics.
 * 
 * Available CLI scripts:
 * - character.cli.ts: Character creation simulation
 * - combat.cli.ts: Combat simulation with enemy selection and state management
 * 
 * Run scripts via npm:
 * - npm run character
 * - npm run combat
 */

// CLI scripts are standalone executables, not exports
export {};
