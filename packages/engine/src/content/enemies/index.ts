/**
 * Enemies content subpath.
 *
 * Bundled enemy registry. Used by the demo CLI; UI consumers can either
 * depend on it directly or substitute their own catalog by passing
 * Enemy instances they construct via `createEnemy`.
 */
export { Disatree_01, EnemyLibrary } from '../../enemy/enemy.library';
