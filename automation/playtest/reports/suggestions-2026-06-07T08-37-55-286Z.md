# Tuning suggestions — 2026-06-07T08-37-55-286Z

Derived from the data report: `tuning-2026-06-07T08-37-55-286Z.md`.
**Focus:** _none (full matrix)_

## Auto-applied changes (in this PR)

_No numeric change improved aggregate health beyond the significance threshold; none applied._

## Considered but not applied

_None._

## Propose-only (needs human judgement)

- Aggregate resolution is within the target band. — Mean resolution 75% sits inside 65–75%; no numeric change recommended.
- Cell l15-strategist-hard is off-band (resolution 0%, defeat 74%). — Inspect this matchup's enemy/loadout content; may need a targeted (non-numeric) change.
- Cell l30-defensive-hard is off-band (resolution 0%, defeat 0%). — Inspect this matchup's enemy/loadout content; may need a targeted (non-numeric) change.
- Cell l50-mixed-normal is off-band (resolution 0%, defeat 0%). — Inspect this matchup's enemy/loadout content; may need a targeted (non-numeric) change.
- Focus tunable combat.skillStatMultiplier (current 0.5). (`combat.skillStatMultiplier`) — Scales skill damage off the scaling stat; a value, not a formula. Direction of improvement is ambiguous from aggregate health; review before tuning.
- Focus tunable combat.defense.advantage (current 2). (`combat.defense.advantage`) — Reward for defending with the advantaged stance. Direction of improvement is ambiguous from aggregate health; review before tuning.
- Focus tunable combat.defense.neutral (current 1.5). (`combat.defense.neutral`) — Defending with a neutral stance. Direction of improvement is ambiguous from aggregate health; review before tuning.
- Focus tunable combat.resourceGen.attackHit (current 3). (`combat.resourceGen.attackHit`) — Tokens generated on a landed attack; paces skill access. Direction of improvement is ambiguous from aggregate health; review before tuning.
- Focus tunable combat.resourceGen.defend (current 5). (`combat.resourceGen.defend`) — Tokens generated on defend; paces defensive playstyles. Direction of improvement is ambiguous from aggregate health; review before tuning.
- Focus tunable combat.friendshipCounterMax (current 3). (`combat.friendshipCounterMax`) — Both-defend rounds needed for a peaceful resolution. Direction of improvement is ambiguous from aggregate health; review before tuning.
- Focus tunable effect.maxIntensity (current 10). (`effect.maxIntensity`) — Ceiling on effect intensity stacking. Direction of improvement is ambiguous from aggregate health; review before tuning.
- Focus tunable effect.maxDuration (current 10). (`effect.maxDuration`) — Ceiling on effect remaining-duration stacking. Direction of improvement is ambiguous from aggregate health; review before tuning.
- Focus tunable progression.statPointsPerLevel (current 3). (`progression.statPointsPerLevel`) — Stat points granted per level; player power curve. Direction of improvement is ambiguous from aggregate health; review before tuning.
- Focus tunable effect.buff_regeneration.duration (current 5). (`effect.buff_regeneration.duration`) — Base duration of the regeneration buff. Direction of improvement is ambiguous from aggregate health; review before tuning.


---

## Balance-analyst enrichment (offline)

> Structured second opinion from the `balance-analyst` subagent, reading the
> full per-cell JSON. Appended for human review — not auto-applied.

**Diagnosis — the matrix is not globally mis-tuned.** Aggregate resolution
(75%) sits at the top edge of the 65–75% band only because two *opposite*
failure modes partially cancel:

- **Mode A — early-game lethality (wants more player power / less enemy power).**
  Four L1 "hard"/"normal" cells die in 2–9 rounds: `l1-defensive-hard` (98%
  defeat), `l1-mixed-hard` (90%), `l1-strategist-hard` (66%).
- **Mode B — mid/late-game stalls (wants the opposite).** Eight L15–L50 cells
  hit the 80-round cap with the player out-damaging the enemy by 50–1000×
  (`l30-defensive-hard` dmgP2E=1071 / 100% timeout, `l50-mixed-normal`
  dmgP2E=204 / 100% timeout, `graveward-keeper` 0% resolution / 100% timeout).

Because the two cliffs pull in opposite directions, **no single global registry
knob moves both toward band** — auto-applying on aggregate-only reasoning would
risk a regression on the timeout cohort. Zero A/B experiments were recorded this
run (`experiments: []`), consistent with the engine keeping nothing.

### Auto-apply candidates: none (with reasons)

- `enemy.statPerLevel` — would help the L1 defeat cells but push the eight
  timeout/stall cells (already 100% timeout with the player winning the damage
  race) further off-band. Opposite-signed. Reject.
- `combat.healthPerStat` / `combat.skillStatMultiplier` — more player TTK/damage
  helps L1 lethality but worsens every stall cell (they already over-damage and
  still time out; the bottleneck is kill-confirmation, not DPS). Reject.
- `progression.statPointsPerLevel` — failures aren't monotonic in level (L1 too
  hard, L15–L30 stall, L50 mostly fine), so no monotone change improves the
  curve. Reject.
- `combat.friendshipCounterMax` / `combat.defense.*` / `combat.resourceGen.*` /
  `effect.*` — the stall cells show friendshipRate=0 and enemyEff≈0: combats
  that resolve by neither kill, friendship, nor defeat. This is a
  termination/closing problem the registry has no knob for. Reject.

### Propose-only (structural — needs human judgement), worst-first

1. **Stall/timeout cohort — kill cannot be confirmed (highest priority).** Eight
   cells hit the 80-round cap with the player dominating damage yet never
   resolving (`l30-defensive-hard`, `l50-mixed-normal`/`graveward-keeper`,
   `l30-mixed-normal`/`iron-covenanter`, `l15-strategist-normal`/
   `reef-barnacle-colony`, `l15-aggressive-hard`/`tolltaker`, `l30-aggressive-hard`/
   `apostate-abbot`, `l30-mixed-hard`/`the-tithewarden`). Common signature:
   enemyEff≈0 and friendshipRate=0 — combats that can't *end*. Inspect these
   enemies for an effective-HP floor / regen / heal loop that outpaces finishing
   damage, and the sim loop for a missing kill-confirmation (or a round cap
   masking a true draw). Formula/content/schema, not a registry value.
2. **`graveward-keeper` — 0% resolution, 100% timeout (`l50-mixed-normal`).**
   Player out-damages 204:1 but never kills — strongly suggests an unkillable
   mechanic (damage cap, revive, or HP-as-effect the kill check ignores).
   Inspect this enemy's definition and any death-trigger effects.
3. **L1 "hard" lethality cliff — defensive/mixed loadouts specifically.**
   `l1-defensive-hard` (98% defeat, yet dmgP2E=54 — out-damages 54:1 and still
   dies) and `l1-mixed-hard` (90%) vs `l1-aggressive-hard` surviving (62%).
   A 54:1 damage lead ending in death points to a defensive-loadout / stance
   interaction, not enemy over-scaling — likely the defensive starting kit
   can't act/heal fast enough at L1, or a burst window one-shots before the lead
   matters. (`l1-strategist-hard` is the one genuine stat fight: dmgP2E=4.5,
   enemyEff=1.34 — plausibly real difficulty.)
4. **`reef-barnacle-colony` and `tolltaker-of-the-ford` flip modes by level**
   (reef: 98% defeat at L1 → 92% timeout at L15; tolltaker: 90% defeat at L1 →
   98% timeout at L15). Inspect for level-scaled HP/regen that outpaces low-level
   damage but becomes an HP sponge at higher levels. Scaling-formula review.
5. **`iron-covenanter` (12% res) and `apostate-abbot` (28% res, 69.5 avg
   rounds)** are HP-sponge timeout enemies (enemyEff≈0, no defeats) — same class
   as #1; review for finish-damage mitigation.

**Analyst confidence:** medium-high on the diagnosis and on auto-applying
nothing (50 runs/cell at 0%/98%/100% are low-variance extremes; the
timeout-vs-defeat split is unambiguous in the damage ratios). The structural
items are actionable now — no higher `--runs` re-run is needed; the signals are
saturated.
